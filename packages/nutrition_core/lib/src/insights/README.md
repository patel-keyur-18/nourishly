# Insights

Deliberately empty in Phase 1.

The insight rule engine (§21.7) turns scored totals into plain-language
strings, subject to the language boundary in §21.7/§21.8 (progress, not
judgement — see `tokens.rules` in the design tokens for the same rule
applied to colour). It depends on scoring, which itself waits on the
nutrition review packet (§0.6). Lands in Phase 3 alongside `scoring/`.
