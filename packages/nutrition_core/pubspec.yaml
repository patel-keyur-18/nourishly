name: nutrition_core
description: >-
  Nourishly's nutrition calculation core: units, nutrient values, target
  curves, aggregation, scoring, and insights. Pure Dart — no Flutter, Drift,
  or network imports (AP-5).
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.13.3

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
