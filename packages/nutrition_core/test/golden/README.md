# Golden vectors

Versioned JSON input/expected-output fixtures for the calculation pipeline
(§20.3, §14.9). Every scoring-ruleset change produces a **new** version;
existing vectors are never mutated, so a regression against an old ruleset
version stays detectable.

Empty until Phase 2/3 add the aggregation and scoring engines this
directory exists to pin down.
